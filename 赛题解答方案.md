# 赛题方案设计

本部分针对 [赛题.md](赛题.md) 给出详细的解决步骤、实现阶段和详细方案。

## 第 1 章 赛题解读与 Gap 分析

### 1.1 赛题要求六阶段

赛题（[赛题.md](赛题.md)）要求构建一个"评测 - 失败归因 - prompt 优化 - 回归验证 - 产物审计"的自动闭环，pipeline 至少包含六个阶段：

1. **Baseline 评测**：对训练集和验证集分别打分，记录每条 case 的 metric 分、pass/fail、失败原因、trace 或 tool trajectory
2. **失败归因**：基于 final response、tool trajectory、trace、rubric、结构化输出等信号归类失败原因（最终回复不匹配/工具调用错误/工具参数错误/route 错误/格式错误/知识召回不足）
3. **PromptIter 优化**：使用 `evaluation/workflow/promptiter` 或等价扩展机制优化至少一个目标 prompt，支持 system prompt、agent instruction、skill 描述或 router prompt
4. **候选验证**：候选 prompt 必须重新跑验证集，并与 baseline 做逐 case delta（新增通过/新增失败/分数提升/分数下降）
5. **接受策略**：实现可配置 gate（验证集总分提升 ≥ 阈值/不能新增 hard fail/关键 case 不能退化/成本或调用次数不能超预算）
6. **审计落盘**：保存每轮候选 prompt、eval result、delta、接受/拒绝理由、运行成本、耗时、随机种子、模型配置或 fake engine 配置

### 1.2 现有能力映射

trpc-agent-go 已具备的能力与赛题六阶段的对应关系：

| 赛题阶段 | 现有能力 | 实现位置 |
|---------|---------|---------|
| Baseline 评测 | ✅ 完整支持 | [evaluation/evaluation.go](evaluation/evaluation.go) `Evaluate` 方法（约 L186-214） |
| 失败归因 | ⚠️ 部分支持 | [evaluation/workflow/promptiter/engine/loss.go](evaluation/workflow/promptiter/engine/loss.go) `loss` 函数（约 L30-82）提取 TerminalLoss，但未做语义归类 |
| PromptIter 优化 | ✅ 完整支持 | [evaluation/workflow/promptiter/engine/engine.go](evaluation/workflow/promptiter/engine/engine.go) `Run` 方法（约 L208-329） |
| 候选验证 | ⚠️ 部分支持 | engine `executeRound`（约 L372-495）已含 validation 评估，但只比较总分，未做逐 case delta |
| 接受策略 | ❌ 不足 | [accept.go](evaluation/workflow/promptiter/engine/accept.go) L33-37 单维 gate（仅 `MinScoreGain`），缺多维 gate |
| 审计落盘 | ⚠️ 部分支持 | [manager/observer.go](evaluation/workflow/promptiter/manager/observer.go) 已持久化完整 RunResult（含每轮 Train/Validation/Losses/Patches/OutputProfile/Acceptance），但未按赛题要求生成 `optimization_report.json` / `.md` 格式 |

确定性运行支持：

| 要求 | 现有能力 | 实现位置 |
|------|---------|---------|
| fake model | ✅ QueueModel | [test/mock_model.go](test/mock_model.go) L23-53 |
| trace mode | ✅ EvalModeTrace | [evaluation/evalset/evalcase.go](evaluation/evalset/evalcase.go) L19-26 |
| deterministic runner | ⚠️ 需包装 | QueueModel 语义是"每次回放整个队列"，需包装为按调用次数消费的 fake engine |

### 1.3 关键 Gap

基于 1.2 的映射，识别出五个关键 Gap：

**Gap 1：失败归因缺失语义归类**

现有 [loss.go](evaluation/workflow/promptiter/engine/loss.go) 的 `loss` 函数（约 L30-82）只提取 `TerminalLoss`（含 `MetricName`/`Severity`/`StepID`/`Loss`），但未做赛题要求的六类语义归类：

- 最终回复不匹配（ROUGE/FinalResponse fail）
- 工具调用错误（ToolTrajectory `matchTool` Name 不匹配）
- 工具参数错误（Arguments 子准则 fail）
- route 错误（Swarm/Team transfer 与预期不符）
- 格式错误（结构化输出 schema 校验 fail）
- 知识召回不足（LLM Rubric + 知识类 rubric fail）

**Gap 2：逐 case delta 缺失**

现有 [accept.go](evaluation/workflow/promptiter/engine/accept.go) **L33-37** 只比较 validation 总分增量：

```go
scoreDelta := candidateScore - baselineScore
Accepted:   scoreDelta >= policy.MinScoreGain,  // L35
```

未做 case 级 delta（新增通过/新增失败/分数提升/分数下降）。赛题要求"区分新增通过、新增失败、分数提升、分数下降"。

**Gap 3：多维 gate 缺失**

现有 `AcceptancePolicy`（约 L13-16）只有 `MinScoreGain` 单字段，无法表达赛题要求的四类 gate：

- 验证集总分提升 ≥ 阈值（现有）
- 不能新增 hard fail（缺失）
- 关键 case 不能退化（缺失）
- 成本或调用次数不能超预算（缺失）

**Gap 4：审计报告格式缺失**

现有 [manager](evaluation/workflow/promptiter/manager/manager.go) 通过 [observer.go](evaluation/workflow/promptiter/manager/observer.go) 的 `applyEvent`（L41-179）已对每种事件（StructureSnapshot/BaselineValidation/RoundTrainEvaluation/RoundLosses/RoundBackward/RoundAggregation/RoundPatchSet/RoundOutputProfile/RoundValidation/RoundCompleted）做完整持久化，每轮的 Train/Validation/Losses/Patches/OutputProfile/Acceptance 全部写入 store（observer.go L38）。但未生成赛题要求的报告格式：

- `optimization_report.json`：baseline/candidate/delta/gate_decision/failure_attribution_stats/cost_latency
- `optimization_report.md`：人读版，含是否接受建议与理由

且未保存赛题要求的完整审计字段：每轮候选 prompt、eval result、delta、接受/拒绝理由、运行成本、耗时、随机种子、模型配置或 fake engine 配置。需新增 AuditReportBuilder 在 manager 持久化数据之上做格式化输出。

**Gap 5：QueueModel 语义不符**

现有 [QueueModel](test/mock_model.go) 的 `GenerateContent`（约 L31-53）语义是"每次调用回放整个队列"（遍历所有 Calls 的所有 Responses 一次性发到 channel，约 L42-50）。这意味着如果 agent 一次会话内多次调用模型，每次都会收到全部预设响应，而非按调用次数消费。赛题需要的 fake engine 应按调用次数顺序消费预设响应。

### 1.4 evolution 澄清

> **重要澄清**：`evolution/` 目录是 **skill 库管理**（从 session 抽取 SKILL.md），**不是** prompt 优化。赛题的"接受门禁"应对应 PromptIter 的 `AcceptancePolicy`，而非 evolution 的 gates。

[evolution/](evolution) 目录的 `types.go`/`gates.go`/`policy.go`/`revision.go`/`service.go` 处理的是 skill 库的版本管理（SKILL.md 的提取、修订、门禁发布），与赛题的 prompt 优化接受策略无关。不要混淆两者。

---

## 第 2 章 总体架构设计

### 2.1 分层架构

方案采用三层架构，最大化复用现有能力，最小化新增代码：

```
┌─────────────────────────────────────────────────────────────┐
│ 编排层（新增）                                                │
│  RegressionLoop（pipeline 主入口，main.go）                   │
│  ├─ 读 configs/（train/validation/metrics/promptiter/baseline）│
│  ├─ 调 baseline_eval → attributor → engine.Run → case_delta  │
│  ├─ → gate → audit                                           │
│  └─ 输出 report/optimization_report.{json,md}                │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│ 新增层（5 个组件）                                            │
│  ├─ FailureAttributor     —— 失败归因（六类语义归类）          │
│  ├─ CaseDeltaCalculator   —— 逐 case delta（四类变化）        │
│  ├─ MultiDimensionGate    —— 多维接受门禁（四类 gate）         │
│  ├─ AuditReportBuilder    —— 审计报告（JSON + MD）            │
│  └─ FakeEngine            —— 包装 QueueModel 为按调用消费     │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│ 复用层（现有）                                                │
│  ├─ evaluation service     —— AgentEvaluator.Evaluate         │
│  ├─ PromptIter engine      —— engine.Run（多轮优化）           │
│  ├─ QueueModel             —— fake model 基础                 │
│  ├─ EvalModeTrace          —— trace mode 跳过真实推理          │
│  └─ Execution Trace        —— loss 提取的终态 step 定位        │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 数据流

```
1. 加载 configs/（train.evalset.json, validation.evalset.json, metrics.json,
                 promptiter.json, baseline prompt, fake model 队列配置）

2. Baseline 评测
   ┌─ evaluation.New(runner=FakeEngine, judgeRunner=FakeEngine, ...)
   ├─ evaluator.Evaluate(ctx, trainEvalSetID)
   ├─ evaluator.Evaluate(ctx, validationEvalSetID)
   └─ 记录 baselineTrainResult, baselineValidationResult

3. 失败归因
   ┌─ FailureAttributor.Attribute(runResult.Rounds[0].Train)  // engine.EvaluationResult
   └─ 输出 []FailureAttribution{CaseID, Category, Reason, Evidence}

4. PromptIter 优化（多轮）
   ┌─ engine.New(ctx, targetAgent, agentEvaluator, backwarder, aggregator, optimizer)
   ├─ RunRequest{Train, Validation, InitialProfile, AcceptancePolicy(多维), StopPolicy,
   │             TargetSurfaceIDs=SurfaceID(), Teacher=FakeEngine, Judge=FakeEngine}
   ├─ engine.Run(ctx, request)
   └─ runResult.Rounds[] 直接取用（含 candidateProfile, patchSet, trainResult,
                                      validationResult, acceptance，无需 Observer 收集）

5. 候选验证与逐 case delta
   ┌─ 取 runResult.BaselineValidation（engine 内部已跑 baseline 验证）
   ├─ 取 runResult.Rounds[最后].Validation（该轮已用 OutputProfile 跑验证，复用而非独立重跑）
   ├─ CaseDeltaCalculator.Compare(baselineValidationResult, candidateValidationResult)
   └─ 输出 []engine.CaseDelta{CaseID, NewlyPassed, NewlyFailed, ScoreImproved, ScoreRegressed}

6. 多维接受门禁（方式 B，engine 内外同一套 gate）
   ┌─ accept(extendedPolicy, baselineScore, candidateScore, caseDeltas, cost, latency)
   └─ 输出 AcceptanceDecision{Accepted, ScoreDelta, Reason, GateResults}（Gate 1/2/3/4 分层判定）

7. 审计报告
   ┌─ AuditReportBuilder.BuildJSON(...)
   ├─ AuditReportBuilder.BuildMD(...)
   └─ 写入 report/optimization_report.{json,md}
```

### 2.3 类型约定（重要）

trpc-agent-go 中存在**三个同名但不同包**的 `EvaluationResult` 类型，本方案各组件必须明确消费哪一个，避免类型混淆导致编译失败：

| 类型 | 定义位置 | 字段结构 | 用途 |
|------|---------|---------|------|
| `evaluation.EvaluationResult` | [evaluation.go L146-153](evaluation/evaluation.go) | `EvalCases []*EvaluationCaseResult` + `EvalResult *evalresult.EvalSetResult` | `AgentEvaluator.Evaluate` 的公共返回值 |
| `engine.EvaluationResult` | [evaluate.go L93-98](evaluation/workflow/promptiter/engine/evaluate.go) | `OverallScore float64` + `EvalSets []EvalSetResult` | PromptIter engine 内部评估结果，`RunResult.Rounds[].Train/Validation` 与 `RunResult.BaselineValidation` 均为此类型 |
| `evalresult.EvalCaseResult` | [evalresult.go L39-58](evaluation/evalresult/evalresult.go) | `FinalEvalStatus` + `OverallEvalMetricResults` + `EvalMetricResultPerInvocation` | 单条 case 的细粒度结果（per-run） |

**本方案约定**：

- **FailureAttributor / CaseDeltaCalculator 直接消费 `engine.EvaluationResult`**（与 [loss.go](evaluation/workflow/promptiter/engine/loss.go) `loss` 函数的访问方式一致），遍历 `result.EvalSets[].Cases[]`，访问 `case.Metrics[]`（非 OverallEvalMetricResults）、`metric.Status`（非 EvalStatus）、`metric.Reason`（非 Details.Reason）。注意：当前源码中的 `engine.CaseResult` **没有** `OverallStatus` 字段，case pass/fail 必须由 `Metrics[].Status` 计算（任一 failed 则 failed；否则任一 passed 则 passed；否则 not_evaluated），不要引用不存在的 `case.OverallStatus`。
- **不使用 `evaluation.AgentEvaluator.Evaluate` 重跑验证集**（公共 API 无法注入 profile，见阶段 4 说明），改为直接复用 `engine.Run` 返回的 `RunResult.Rounds[].Validation`（已是 `engine.EvaluationResult` 类型）。
- **status 包路径**：状态枚举位于 [evaluation/status](evaluation/status) 包，常量为 `status.EvalStatusPassed` / `status.EvalStatusFailed` / `status.EvalStatusNotEvaluated`（非 `evalstatus.Passed`）。

`engine.EvaluationResult` 的内部结构（[evaluate.go L68-98](evaluation/workflow/promptiter/engine/evaluate.go)）：

```go
type CaseResult struct {
    EvalSetID   string
    EvalCaseID  string
    Trace       *atrace.Trace
    Metrics     []MetricResult   // ← 含 Status/Reason/Score/MetricName
    // 注意：当前源码无 OverallStatus 字段，需通过 Metrics[] 计算 case 状态。
    // ...
}

type EvalSetResult struct {
    EvalSetID string
    Cases     []CaseResult       // ← 遍历入口
    // ...
}

type EvaluationResult struct {
    OverallScore float64
    EvalSets     []EvalSetResult
}

type MetricResult struct {
    MetricName string
    Score      float64
    Status     status.EvalStatus
    Reason     string
}
```

### 2.4 目录建议

按赛题"建议放在 `examples/evaluation/promptiter_regression_loop/`、`examples/optimization/eval_promptiter_loop/` 或等价目录"的要求，推荐结构：

```
examples/evaluation/promptiter_regression_loop/
├── main.go                          # pipeline 入口
├── configs/
│   ├── train.evalset.json           # 3 条训练 case
│   ├── validation.evalset.json      # 3 条验证 case（含可优化成功/优化无效/验证集退化三类）
│   ├── metrics.json                 # ROUGE + ToolTrajectory + LLM Rubric
│   ├── promptiter.json              # PromptIter 配置（MaxRounds, MinScoreGain, TargetScore）
│   ├── baseline_prompt.txt          # baseline prompt 源文件
│   └── fake_model_queue.json        # fake model 预设响应队列
├── report/
│   ├── optimization_report.json     # 结构化报告
│   └── optimization_report.md       # 人读版报告
├── internal/
│   ├── failure_attribution.go       # FailureAttributor
│   ├── case_delta.go                # CaseDeltaCalculator
│   ├── gate.go                      # MultiDimensionGate
│   ├── audit_report.go              # AuditReportBuilder
│   └── fake_engine.go               # FakeEngine（包装 QueueModel）
├── failure_attribution_test.go      # 失败归因单测
├── case_delta_test.go               # 逐 case delta 单测
├── gate_test.go                     # gate 决策单测
├── audit_report_test.go             # 报告生成单测
└── README.md                        # 说明文档
```

---

## 第 3 章 详细实现阶段
### 3.0 推荐实施顺序（重要）

> 下方阶段 0-8 是**架构逻辑顺序**，但实现时应采用"先跑通最小闭环，再逐层加功能"的策略。FakeEngine 是最大阻塞项——没有它任何代码都无法无 key 运行验证。

**实施步骤与阶段映射**：

| 实施步骤 | 对应文档阶段 | 内容 | 优先级 |
|---------|------------|------|--------|
| **步骤 1** 骨架目录 + 数据文件 | 阶段 0 | 创建 `examples/evaluation/promptiter_regression_loop/` 目录、6 条 evalset case（3 训练 + 3 验证，含三类情况）、metrics.json、baseline_prompt.txt | 高 |
| **步骤 2** FakeEngine | 阶段 7（提前） | 实现按调用次数消费的 FakeEngine，五角色各建独立实例。**先跑通最小 syncrun 变体验证五角色响应 schema** | 高（阻塞项） |
| **步骤 3** 最小 pipeline 端到端 | 阶段 3 | 用 FakeEngine + engine.Run 跑通完整 PromptIter，确认 `runResult.Rounds[]`/`BaselineValidation`/`AcceptedProfile` 数据通路 | 高 |
| **步骤 4** 失败归因 + case delta | 阶段 2 + 阶段 4 | FailureAttributor（六类归类）+ CaseDeltaCalculator（逐 case 比较），消费 `engine.EvaluationResult` | 中 |
| **步骤 5** 多维 gate | 阶段 5 | 方式 B 扩展 [accept.go](evaluation/workflow/promptiter/engine/accept.go) 的 `AcceptancePolicy`/`accept`，并把 `CaseDelta`/预算类型与计算逻辑放入 `evaluation/workflow/promptiter/engine` 包，避免 engine 反向依赖 example/internal | 中 |
| **步骤 6** 审计报告 + main 串联 | 阶段 6 + 阶段 1 | AuditReportBuilder 生成 json/md，main.go 串联完整流程 | 中 |
| **步骤 7** 单测 + 验收自验 | 阶段 8 | 覆盖 gate/delta/归因/报告四类单测，构造 ≥10 条离线 case 验证 gate 决策准确率 | 中 |

**关键原则**：
1. **FakeEngine 最先做**——它是无 key 运行的前提，也是审查报告标注的高风险点
2. **先跑通最小闭环再逐层加功能**——避免到最后才发现类型不通
3. **baseline 评测无需独立实现**——直接复用 `runResult.BaselineValidation`（engine 内部已跑）
4. **gate 用方式 B**——扩展 accept.go 而非外层包装，避免状态分裂；gate 所需的 delta/预算类型必须位于 engine 包内
5. **fake_model_queue.json 的"剧情脚本"需先设计**——确保 6 条 case 产出三类结果（可优化成功/优化无效/验证集退化）

### 阶段 0：环境与样例

**目标**：准备 pipeline 运行所需的全部输入。

**步骤**：

1. 创建 `configs/train.evalset.json`，包含 3 条训练 case，覆盖：
   - 可优化成功的 case（baseline fail，优化后 pass）
   - 优化无效的 case（baseline fail，优化后仍 fail）
   - 已通过 case（baseline pass，优化后仍 pass）

2. 创建 `configs/validation.evalset.json`，包含 3 条验证 case，覆盖赛题要求的三类情况：
   - 可优化成功（训练集优化对验证集也有效）
   - 优化无效（训练集优化对验证集无效果）
   - 验证集退化（训练集提升但验证集退化 —— 过拟合场景）

3. 创建 `configs/metrics.json`，配置三类 metric：
   - ROUGE（最终回复匹配）
   - ToolTrajectory（工具调用轨迹，含 ordered/unordered 两种模式）
   - LLM Rubric（知识召回/格式正确性等）

4. 创建 `configs/promptiter.json`，配置：
   - `MaxRounds`：最大优化轮数（如 5）
   - `MinScoreGain`：最小分数提升阈值（如 0.01）
   - `TargetScore`：目标分数（如 0.9）
   - `TargetSurfaceIDs`：可优化 surface（Instruction/GlobalInstruction/FewShot/Model）

5. 创建 `configs/baseline_prompt.txt`，写入待优化的 baseline prompt

6. 创建 `configs/fake_model_queue.json`，预设 fake model 响应队列：
   - candidate model 的响应（多轮，每轮生成不同的 patch 建议）
   - judge model 的响应（评分）
   - worker model 的响应（backward/aggregation/optimization）

### 阶段 1：Baseline 评测

**目标**：对训练集和验证集分别打分，记录每条 case 的 metric 分、pass/fail、失败原因、trace、tool trajectory。

**步骤**：

1. 构造 FakeEngine（见阶段 7），注入 `evaluation.New`。这个 evaluator 是 PromptIter engine 的依赖，主链路由 `engine.Run` 调用它完成 baseline validation 与每轮 train/validation 评测：

```go
// 示意代码（非 file 链接，对应文件尚不存在）
evaluator, err := evaluation.New(
    appName,
    fakeEngineRunner,  // 用 FakeEngine 包装 QueueModel
    evaluation.WithJudgeRunner(fakeJudgeRunner),
    evaluation.WithNumRuns(1),
    evaluation.WithEvalSetManager(evalSetManager),
    evaluation.WithMetricManager(metricManager),
    evaluation.WithRunDetailsEnabled(true),  // 保留 trace 和 tool trajectory
)
```

2. 主链路不要额外调用 `Evaluate` 重跑 baseline，直接从 `engine.Run` 结果取值：

```go
runResult, err := engineInstance.Run(ctx, request)
baselineTrainResult := runResult.Rounds[0].Train          // baseline profile 的训练集评测
baselineValidationResult := runResult.BaselineValidation  // baseline profile 的验证集评测
```

这里要求 `MaxRounds >= 1`，否则没有 `Rounds[0].Train` 可用于 baseline train 归因。

3. 若调试时确实需要独立调用公共 evaluator，可从 `evaluation.EvaluationResult.EvalCases` 提取每条 case 的（字段为 `evaluation.EvaluationCaseResult`，见 [evaluation.go L156-162](evaluation/evaluation.go)）：
   - pass/fail（`OverallStatus`，类型 `status.EvalStatus`）
   - metric 分与原因（`MetricResults[]`，含 `Score`/`Status`/`Reason`）
   - trace（通过 `WithRunDetailsEnabled(true)` + Execution Trace）
   - tool trajectory（通过 `EvalResult.EvalCaseResults[].EvalMetricResultPerInvocation` 的 Actual/Expected Invocation 的 `Tools` 字段）

> **类型边界说明**：主链路不要在 `evaluation.EvaluationResult` 与 `engine.EvaluationResult` 之间来回转换。阶段 2 的 FailureAttributor 与阶段 4 的 CaseDeltaCalculator 消费 `engine.EvaluationResult`（来自 `runResult.Rounds[].Train/Validation`，见 [2.3 类型约定](#23-类型约定重要)）。baseline 的失败归因使用 `runResult.Rounds[0].Train`（首轮 train 评估用 baseline profile，等价于 baseline train 结果），baseline 验证使用 `runResult.BaselineValidation`。

**复用现有代码**：

- [evaluation/evaluation.go](evaluation/evaluation.go) `Evaluate` 方法（约 L186-214）
- [evaluation/service/local/local.go](evaluation/service/local/local.go) `evaluatePerCase`（约 L382-515）
- [evaluation/evalresult/evalresult.go](evaluation/evalresult/evalresult.go) `EvalCaseResult`（约 L39-58）

### 阶段 2：失败归因（FailureAttributor）

**目标**：基于 final response、tool trajectory、trace、rubric、结构化输出等信号归类失败原因。

**输入**：baseline `engine.EvaluationResult`（含每条 case 的 `Trace` 与 `Metrics`，见 [evaluate.go L68-98](evaluation/workflow/promptiter/engine/evaluate.go)）

**规则归类**（基于 [engine/loss.go](evaluation/workflow/promptiter/engine/loss.go) `loss` 函数 L30-82 的 `TerminalLoss` 提取机制扩展）：

> **类型说明**：本函数直接消费 `engine.EvaluationResult`（非 `evaluation.EvaluationResult`），遍历 `result.EvalSets[].Cases[]`，与 [loss.go](evaluation/workflow/promptiter/engine/loss.go) L35-36 的访问方式一致。字段映射见 [2.3 类型约定](#23-类型约定重要)。

```go
// 示意代码（非 file 链接）
type FailureCategory string

const (
    FailureCategoryFinalResponseMismatch FailureCategory = "final_response_mismatch"  // 最终回复不匹配
    FailureCategoryToolCallError         FailureCategory = "tool_call_error"          // 工具调用错误
    FailureCategoryToolArgError          FailureCategory = "tool_arg_error"           // 工具参数错误
    FailureCategoryRouteError            FailureCategory = "route_error"              // route 错误
    FailureCategoryFormatError           FailureCategory = "format_error"             // 格式错误
    FailureCategoryKnowledgeRecall       FailureCategory = "knowledge_recall"         // 知识召回不足
)

type FailureAttribution struct {
    CaseID   string
    Category FailureCategory
    Reason   string
    Evidence string  // 具体证据（如不匹配的工具名、缺失的 schema 字段等）
}

// Attribute 接收 engine.EvaluationResult（RunResult.BaselineValidation 或 Rounds[].Train）。
// 每个 case 自带 Trace（engine.CaseResult.Trace），无需外部传入全局 trace。
func (a *FailureAttributor) Attribute(
    result *promptiterengine.EvaluationResult,
) []FailureAttribution {
    var attributions []FailureAttribution
    for _, evalSet := range result.EvalSets {
        for _, caseResult := range evalSet.Cases {
            // 只归因失败 case。engine.CaseResult 无 OverallStatus，需由 Metrics[] 汇总。
            caseStatus := caseStatusFromMetrics(caseResult.Metrics)
            if caseStatus == status.EvalStatusPassed {
                continue
            }
            // route 错误：检查 case 自带 Trace 的 transfer 记录
            if isRouteError(caseResult.Trace) {
                attributions = append(attributions, FailureAttribution{
                    CaseID:   caseResult.EvalCaseID,
                    Category: FailureCategoryRouteError,
                    Reason:   "unexpected agent transfer",
                    Evidence: extractTransferEvidence(caseResult.Trace),
                })
            }
            // 按 metric 类型归类（Metrics[] 含 Status/Reason/MetricName）
            for _, metric := range caseResult.Metrics {
                if metric.Status != status.EvalStatusFailed {
                    continue
                }
                switch {
                case isRougeMetric(metric):
                    // ROUGE/FinalResponse fail → 最终回复不匹配
                    attributions = append(attributions, FailureAttribution{
                        CaseID:   caseResult.EvalCaseID,
                        Category: FailureCategoryFinalResponseMismatch,
                        Reason:   metric.Reason,
                        Evidence: extractResponseDiff(metric),
                    })
                case isToolTrajectoryMetric(metric):
                    // ToolTrajectory fail → 进一步区分工具调用错误/参数错误
                    category, evidence := diagnoseToolFailure(metric)
                    attributions = append(attributions, FailureAttribution{
                        CaseID:   caseResult.EvalCaseID,
                        Category: category,
                        Reason:   metric.Reason,
                        Evidence: evidence,
                    })
                case isLLMRubricMetric(metric):
                    // LLM Rubric fail → 进一步区分格式错误/知识召回不足
                    category, evidence := diagnoseRubricFailure(metric)
                    attributions = append(attributions, FailureAttribution{
                        CaseID:   caseResult.EvalCaseID,
                        Category: category,
                        Reason:   metric.Reason,
                        Evidence: evidence,
                    })
                }
            }
        }
    }
    return attributions
}
```

`caseStatusFromMetrics` 不应导入 `evaluation/internal/status`（example 不能合法导入 internal 包），在当前 example 或 engine 包内实现同等规则即可：

```go
func caseStatusFromMetrics(metrics []promptiterengine.MetricResult) status.EvalStatus {
    hasPassed := false
    for _, metric := range metrics {
        switch metric.Status {
        case status.EvalStatusFailed:
            return status.EvalStatusFailed
        case status.EvalStatusPassed:
            hasPassed = true
        }
    }
    if hasPassed {
        return status.EvalStatusPassed
    }
    return status.EvalStatusNotEvaluated
}
```

**诊断辅助函数**：

- `isRougeMetric(metric)` / `isToolTrajectoryMetric(metric)` / `isLLMRubricMetric(metric)`：基于 `metric.MetricName` 前缀或 metric 配置（criterion 类型）判定 metric 类别。建议在初始化时从 [metricManager](evaluation/metric) 加载 metric 配置，缓存 `MetricName → criterionType` 映射，避免仅靠名称猜。
- `diagnoseToolFailure(metric)`：解析 `metric.Reason`（含 [tooltrajectory.go](evaluation/metric/criterion/tooltrajectory/tooltrajectory.go) `matchTool` L150-166 返回的错误信息）—— 若 Reason 含 "name mismatch"（L189-194 的 `Name.Match` 失败）则 `ToolCallError`，若含 "arguments mismatch"（L197-204 的 `Arguments.Match` 失败）则 `ToolArgError`。为提升鲁棒性，可重新调用 `ToolTrajectoryStrategy.Match`（L187-215）分别检查 Name 与 Arguments，而非仅靠字符串匹配。
- `diagnoseRubricFailure(metric)`：检查 LLM Rubric 的 rubric 内容（从 metric 配置或 `EvalCaseRubric.Content.Text` 获取）—— 若 rubric 含"格式"/"schema"关键词则 `FormatError`，若含"知识"/"召回"/"事实"关键词则 `KnowledgeRecall`。
- `isRouteError(trace)` / `extractTransferEvidence(trace)`：遍历 `trace.Steps`，检查是否有非预期的 agent transfer 记录（如 Swarm/Team 的 transfer 与预期不符）。

**输出**：`[]FailureAttribution{CaseID, Category, Reason, Evidence}`，每个失败 case 至少一个归因。

**复用现有代码**：

- [engine/loss.go](evaluation/workflow/promptiter/engine/loss.go) `loss` 函数（约 L30-82）提取 TerminalLoss
- [engine/loss.go](evaluation/workflow/promptiter/engine/loss.go) `traceTerminalStepIDs`（约 L207-254）通过拓扑找终态 step
- [tooltrajectory.go](evaluation/metric/criterion/tooltrajectory/tooltrajectory.go) `matchTool`（约 L151-166）的工具比较逻辑

### 阶段 3：PromptIter 优化

**目标**：复用现有 PromptIter engine 执行多轮优化。

**前置：构造 targetAgent 与 InitialProfile**

`engine.New` 需要 `targetAgent agent.Agent`，该 agent 的 instruction surface 携带 baseline prompt。参考 syncrun 示例 [engine.go L132](examples/evaluation/promptiter/syncrun/engine.go) 的 `newCandidateAgent(candidateModel, cfg.CandidateInstruction)`，用 FakeEngine 替换 candidateModel：

```go
// 示意代码（非 file 链接）
candidateModel := fakeengine.NewModel("candidate", candidateResponseQueue)
targetAgent, err := newCandidateAgent(candidateModel, baselinePromptText)  // baseline_prompt.txt 内容
```

`InitialProfile` 可设为 `nil`（engine 会用 targetAgent 的当前 surface 作为初始 profile，见 [engine.go L236-239](evaluation/workflow/promptiter/engine/engine.go) `normalizeProfile`）。若需显式指定，参考 `iprofile.Clone` 与 [promptiter.Profile](evaluation/workflow/promptiter) 结构构造 SurfaceEntries。

**步骤**：

1. 构造 PromptIter runtime（参考 syncrun [engine.go L116-215](examples/evaluation/promptiter/syncrun/engine.go) `buildPromptIterRuntime`）。注意 `engine.New` **首参为 ctx**：

```go
// 示意代码
engineInstance, err := promptiterengine.New(
    ctx,                // ← 必填，syncrun engine.go L196 确认
    targetAgent,
    agentEvaluator,
    backwarderInstance,
    aggregatorInstance,
    optimizerInstance,
)
```

2. 构造 `RunRequest`（参考 syncrun [engine.go L217-257](examples/evaluation/promptiter/syncrun/engine.go) `buildRunRequest`）。注意四个字段的正确类型：

```go
// 示意代码
targetScore := 0.9  // StopPolicy.TargetScore 是 *float64，需取地址
targetSurfaceID := astructure.SurfaceID(
    candidateAgentName,
    astructure.SurfaceTypeInstruction,  // ← 用 SurfaceID() 计算，非字面量 "Instruction"
)
request := &promptiterengine.RunRequest{
    Train: []promptiterengine.EvalSetInput{                 // ← []EvalSetInput 切片
        {EvalSetID: trainEvalSetID},
    },
    Validation: []promptiterengine.EvalSetInput{
        {EvalSetID: validationEvalSetID},
    },
    InitialProfile: nil,  // 用 targetAgent 当前 profile
    Teacher:        fakeEngineRunner,  // 可选，nil 时用 agentEvaluator 的 runner
    Judge:          fakeJudgeRunner,   // 可选
    EvaluationOptions: promptiterengine.EvaluationOptions{  // ← 结构体，非 []Option
        EvalCaseParallelism:              4,
        EvalCaseParallelInferenceEnabled: false,  // fake 模式下串行即可
        EvalCaseParallelEvaluationEnabled: false,
    },
    AcceptancePolicy: promptiterengine.AcceptancePolicy{
        MinScoreGain: 0.01,
        // 多维 gate 字段见阶段 5（方式 B 扩展）
    },
    StopPolicy: promptiterengine.StopPolicy{
        MaxRoundsWithoutAcceptance: 3,
        TargetScore:                &targetScore,  // ← *float64 指针
    },
    MaxRounds:        5,
    TargetSurfaceIDs: []string{targetSurfaceID},  // ← SurfaceID() 计算结果
}
```

3. 执行 `engine.Run`。`RunResult.Rounds` 已自带每轮完整 `RoundResult`（含 Train/Validation/Losses/Patches/OutputProfile/Acceptance），**无需 Observer 收集**：

```go
runResult, err := engineInstance.Run(ctx, request)
if err != nil { ... }
// runResult.Rounds[] 直接取用，runResult.AcceptedProfile 为最终接受 profile（若有）
// runResult.BaselineValidation 为 baseline 验证结果（engine.EvaluationResult）
```

> **为什么不用 Observer**：`EventKindRoundCompleted` 的 payload 是 `*RoundCompleted` 摘要（[event.go L59-70](evaluation/workflow/promptiter/engine/event.go)），不含完整 RoundResult。而 `engine.Run` 返回的 `*RunResult` 已在 [engine.go L321](evaluation/workflow/promptiter/engine/engine.go) `result.Rounds = append(result.Rounds, *roundResult)` 填充了每轮完整数据，直接取用即可。Observer 仅用于流式监控/持久化（如 manager 的用法），本方案无需。

4. 从 `runResult` 获取 `acceptedProfile`（最终接受的候选 prompt，可能为 nil 表示无轮次被接受）或取最后一轮 `runResult.Rounds[len-1].OutputProfile` 作为最终候选。

**复用现有代码**：

- [engine.New](evaluation/workflow/promptiter/engine/engine.go) L170-196（首参 ctx）
- [engine.Run](evaluation/workflow/promptiter/engine/engine.go) L208-329
- [engine.executeRound](evaluation/workflow/promptiter/engine/engine.go) L372-495（七步流程）
- [engine.go L124](evaluation/workflow/promptiter/engine/engine.go) `RunResult.Rounds []RoundResult`（直接取用）

**TargetSurfaceIDs 说明**：参考 [server/promptiter/handler.go](server/promptiter/handler.go) `validateTargetSurfaceIDs`（L229-259）与 `isSupportedTargetSurfaceType`（L261-271），四类 surface 可优化（常量定义见 [structure.go L46-52](agent/structure/structure.go)）：

- `astructure.SurfaceTypeInstruction`（值 `"instruction"`）—— 对应 agent instruction（system prompt / agent instruction）
- `astructure.SurfaceTypeGlobalInstruction`（值 `"global_instruction"`）—— 全局指令
- `astructure.SurfaceTypeFewShot`（值 `"few_shot"`）—— few-shot 示例
- `astructure.SurfaceTypeModel`（值 `"model"`）—— 模型配置

surface ID 需用 [astructure.SurfaceID(nodeID, surfaceType)](agent/structure/structure.go) 计算（格式 `nodeID + "#" + surfaceType`），不能直接写字面量。这覆盖赛题要求的 system prompt / agent instruction / skill 描述 / router prompt（前两者直接对应，FewShot/Model 为扩展）。

### 阶段 4：候选验证与逐 case delta

**目标**：基于 engine 产出的验证结果，与 baseline 做逐 case delta。

> **重要**：不使用 `evaluation.AgentEvaluator.Evaluate` 重跑验证集——公共 API 的 `WithRunOptions`（[options.go L212](evaluation/options.go)）接收 `agent.RunOption`（运行时选项），**无法注入 profile**。profile 应用是 engine 内部行为（[engine.go L461](evaluation/workflow/promptiter/engine/engine.go) `applyPatchSet` → [evaluate.go L47](evaluation/workflow/promptiter/engine/evaluate.go) `EvaluationRequest.Profile`，`engine.evaluate` 为非导出方法）。因此改为直接复用 engine 的验证结果。

**步骤**：

1. 从 `runResult` 取 baseline 与 candidate 的验证结果（均为 `engine.EvaluationResult` 类型）：

```go
// 示意代码
baselineValidation := runResult.BaselineValidation          // engine.Run 内部已跑 baseline 验证
// candidate 取最后一轮的 validation（该轮已用 OutputProfile 跑验证）
lastRound := runResult.Rounds[len(runResult.Rounds)-1]
candidateValidation := lastRound.Validation
```

2. 实现 `CaseDeltaCalculator`，逐 case 比较 baseline vs candidate（均消费 `engine.EvaluationResult`，遍历 `EvalSets[].Cases[]`）：

```go
// 示意代码
type CaseDeltaType string

const (
    CaseDeltaNewlyPassed    CaseDeltaType = "newly_passed"     // 新增通过
    CaseDeltaNewlyFailed    CaseDeltaType = "newly_failed"     // 新增失败
    CaseDeltaScoreImproved  CaseDeltaType = "score_improved"   // 分数提升
    CaseDeltaScoreRegressed CaseDeltaType = "score_regressed"  // 分数下降
    CaseDeltaUnchanged      CaseDeltaType = "unchanged"        // 无变化
)

type CaseDelta struct {
    CaseID          string
    Type            CaseDeltaType
    BaselineScore   float64
    CandidateScore  float64
    ScoreDelta      float64
    BaselineStatus  status.EvalStatus
    CandidateStatus status.EvalStatus
}

// Compare 接收 engine.EvaluationResult，遍历 EvalSets[].Cases[]。
// 参考 [loss.go L35-36](evaluation/workflow/promptiter/engine/loss.go) 的遍历方式。
func (c *CaseDeltaCalculator) Compare(
    baseline *promptiterengine.EvaluationResult,
    candidate *promptiterengine.EvaluationResult,
) []CaseDelta {
    baselineByCaseID := indexCasesByCaseID(baseline)   // 遍历 baseline.EvalSets[].Cases[]
    candidateByCaseID := indexCasesByCaseID(candidate)
    var deltas []CaseDelta
    for caseID, baseCase := range baselineByCaseID {
        candCase := candidateByCaseID[caseID]
        baseScore := averageMetricScore(baseCase)   // 对 engine.CaseResult.Metrics[] 求均
        candScore := averageMetricScore(candCase)
        baseStatus := caseStatusFromMetrics(baseCase.Metrics)
        candStatus := caseStatusFromMetrics(candCase.Metrics)
        delta := CaseDelta{
            CaseID:          caseID,
            BaselineScore:   baseScore,
            CandidateScore:  candScore,
            ScoreDelta:      candScore - baseScore,
            BaselineStatus:  baseStatus,
            CandidateStatus: candStatus,
        }
        switch {
        case baseStatus != status.EvalStatusPassed &&
             candStatus == status.EvalStatusPassed:
            delta.Type = CaseDeltaNewlyPassed
        case baseStatus == status.EvalStatusPassed &&
             candStatus != status.EvalStatusPassed:
            delta.Type = CaseDeltaNewlyFailed
        case candScore > baseScore:
            delta.Type = CaseDeltaScoreImproved
        case candScore < baseScore:
            delta.Type = CaseDeltaScoreRegressed
        default:
            delta.Type = CaseDeltaUnchanged
        }
        deltas = append(deltas, delta)
    }
    return deltas
}

// indexCasesByCaseID 遍历 engine.EvaluationResult.EvalSets[].Cases[] 建立 CaseID 索引。
func indexCasesByCaseID(result *promptiterengine.EvaluationResult) map[string]promptiterengine.CaseResult {
    index := make(map[string]promptiterengine.CaseResult)
    for _, evalSet := range result.EvalSets {
        for _, c := range evalSet.Cases {
            index[c.EvalCaseID] = c
        }
    }
    return index
}

// averageMetricScore 对 engine.CaseResult.Metrics[] 求均（跳过 NotEvaluated）。
// 逻辑参考 [runner/bestofn/evaluation.go L480](runner/bestofn/evaluation.go) averageMetricScore，
// 但后者接收 evalresult.EvalCaseResult，此处适配 engine.CaseResult.Metrics。
func averageMetricScore(caseResult promptiterengine.CaseResult) float64 {
    total, count := 0.0, 0
    for _, m := range caseResult.Metrics {
        if m.Status == status.EvalStatusNotEvaluated {
            continue
        }
        total += m.Score
        count++
    }
    if count == 0 {
        return 0
    }
    return total / float64(count)
}
```

**落地位置要求**：

- 如果只是生成报告，可将 `CaseDeltaCalculator` 放在 `examples/evaluation/promptiter_regression_loop/internal/`。
- 如果阶段 5 采用方式 B 让 engine 内部 gate 直接使用 case delta，则 `CaseDelta` 类型、`CompareCaseDeltas`、`caseStatusFromMetrics` 必须放到 `evaluation/workflow/promptiter/engine` 包内（例如新增 `case_delta.go`），否则 engine 包无法合法依赖 example 的 `internal` 包。

**复用现有代码**：

- [evaluate.go L68-98](evaluation/workflow/promptiter/engine/evaluate.go) `CaseResult`/`MetricResult` 结构（字段 `Metrics`/`MetricName`/`Score`/`Status`/`Reason`；无 `OverallStatus`）
- [runner/bestofn/evaluation.go L480](runner/bestofn/evaluation.go) `averageMetricScore` 的求均逻辑（参考实现，需适配 engine.CaseResult）

### 阶段 5：多维接受门禁（MultiDimensionGate）

**目标**：用方式 B 替换 [accept.go](evaluation/workflow/promptiter/engine/accept.go) L33-37 单维判定，在 engine 内部实现四类 gate，避免外层包装造成接受状态分裂。

**步骤**：

1. 定义多维 gate 配置和决策结构。**这些类型若被 `accept.go` 使用，必须定义在 `evaluation/workflow/promptiter/engine` 包内**；example 的 `internal/gate.go` 只能做报告汇总或展示，不应成为 engine 的依赖：

```go
// 示意代码：位于 evaluation/workflow/promptiter/engine
type AcceptancePolicy struct {
    MinScoreGain     float64      // 原字段保留（Gate 1 阈值）
    NoNewHardFail    bool         // Gate 2 开关
    CriticalCaseIDs  []string     // Gate 3 关键 case
    BudgetConstraint *BudgetLimit // Gate 4 预算
}

type BudgetLimit struct {
    MaxCost       float64       // 最大成本
    MaxAPICalls   int           // 最大 API 调用次数
    MaxLatency    time.Duration // 最大耗时
}

type BudgetUsage struct {
    Cost     float64
    APICalls int
    Latency  time.Duration
}

type GateResult struct {
    GateName string
    Passed   bool
    Reason   string
}

type AcceptanceDecision struct {
    Accepted    bool
    ScoreDelta  float64
    Reason      string
    GateResults []GateResult
}
```

2. 实现 gate 评估逻辑。若采用方式 B，建议让 `accept` 返回扩展后的 `AcceptanceDecision`，报告直接读取 `RoundResult.Acceptance.GateResults`，避免外层再维护一套 `GateDecision` 与 engine 状态不一致：

```go
// 示意代码
func evaluateGates(
    policy AcceptancePolicy,
    baselineScore, candidateScore float64,
    caseDeltas []CaseDelta,
    usage BudgetUsage,
) *AcceptanceDecision {
    var results []GateResult
    var reasons []string
    allPassed := true

    // Gate 1: 验证集总分提升
    scoreDelta := candidateScore - baselineScore
    scorePassed := scoreDelta >= policy.MinScoreGain
    results = append(results, GateResult{
        GateName: "ValidationScoreGain",
        Passed:   scorePassed,
        Reason:   fmt.Sprintf("scoreDelta=%.4f, threshold=%.4f", scoreDelta, policy.MinScoreGain),
    })
    if !scorePassed {
        allPassed = false
        reasons = append(reasons, "validation score gain insufficient")
    }

    // Gate 2: 不新增 hard fail
    if policy.NoNewHardFail {
        newFails := filterNewlyFailed(caseDeltas)
        noNewFailPassed := len(newFails) == 0
        results = append(results, GateResult{
            GateName: "NoNewHardFail",
            Passed:   noNewFailPassed,
            Reason:   fmt.Sprintf("newly failed cases: %d", len(newFails)),
        })
        if !noNewFailPassed {
            allPassed = false
            reasons = append(reasons, fmt.Sprintf("%d newly failed cases", len(newFails)))
        }
    }

    // Gate 3: 关键 case 不得退化
    if len(policy.CriticalCaseIDs) > 0 {
        criticalRegressed := filterCriticalRegressed(caseDeltas, policy.CriticalCaseIDs)
        criticalPassed := len(criticalRegressed) == 0
        results = append(results, GateResult{
            GateName: "CriticalCasePreserve",
            Passed:   criticalPassed,
            Reason:   fmt.Sprintf("regressed critical cases: %d", len(criticalRegressed)),
        })
        if !criticalPassed {
            allPassed = false
            reasons = append(reasons, fmt.Sprintf("%d critical cases regressed", len(criticalRegressed)))
        }
    }

    // Gate 4: 预算约束
    if policy.BudgetConstraint != nil {
        budgetPassed := usage.Cost <= policy.BudgetConstraint.MaxCost &&
            usage.APICalls <= policy.BudgetConstraint.MaxAPICalls &&
            usage.Latency <= policy.BudgetConstraint.MaxLatency
        results = append(results, GateResult{
            GateName: "BudgetConstraint",
            Passed:   budgetPassed,
            Reason:   fmt.Sprintf("cost=%.2f/%.2f, calls=%d/%d, latency=%v/%v",
                usage.Cost, policy.BudgetConstraint.MaxCost,
                usage.APICalls, policy.BudgetConstraint.MaxAPICalls,
                usage.Latency, policy.BudgetConstraint.MaxLatency),
        })
        if !budgetPassed {
            allPassed = false
            reasons = append(reasons, "budget exceeded")
        }
    }

    reason := "all gates passed"
    if !allPassed {
        reason = strings.Join(reasons, "; ")
    }
    return &AcceptanceDecision{
        Accepted:    allPassed,
        ScoreDelta:  scoreDelta,
        Reason:      reason,
        GateResults: results,
    }
}
```

3. **与现有 engine 集成**：有两种方式：

   - **方式 A（外层包装）**：engine 内部仍用单维 gate，外部 MultiDimensionGate 做最终决策。优点：不修改 engine 代码；**缺点：存在状态分裂风险**——engine 在 [engine.go L285-291](evaluation/workflow/promptiter/engine/engine.go) 一旦内部 `Acceptance.Accepted=true`，立即更新 `acceptedProfile` 与 `acceptedValidationScore` 并据此推进后续轮次，外层 gate 随后拒绝无法回滚 engine 状态，导致"外层拒绝但 engine 已采纳"的不一致。
   - **方式 B（替换，推荐）**：扩展 `AcceptancePolicy` 结构体增加多维字段，让 engine 内外用同一套 gate。优点：状态一致，过拟合场景能正确拒绝；缺点：需修改 [accept.go](evaluation/workflow/promptiter/engine/accept.go)（仅 44 行，改动可控）。

   **推荐方式 B**，扩展 `AcceptancePolicy` 与 `accept` 方法：

```go
// 示意代码：扩展 evaluation/workflow/promptiter/engine/accept.go
// accept 方法扩展签名，注入 caseDeltas 与 BudgetUsage（由 executeRound 计算后传入）
func (e *engine) accept(
    policy AcceptancePolicy,
    baselineScore, candidateScore float64,
    caseDeltas []CaseDelta,         // 新增：逐 case delta
    usage BudgetUsage,              // 新增：成本/调用次数/耗时
) *AcceptanceDecision {
    return evaluateGates(policy, baselineScore, candidateScore, caseDeltas, usage)
}
```

   修改点：
   - 在 `evaluation/workflow/promptiter/engine` 新增 `case_delta.go`，放置 `CaseDelta`、`CompareCaseDeltas`、`caseStatusFromMetrics`、`averageMetricScore`。
   - 修改 [accept.go](evaluation/workflow/promptiter/engine/accept.go) 的 `AcceptancePolicy` 结构体（L13-16）与 `accept` 方法（L28-44），增加多维字段与参数，并可在 `AcceptanceDecision` 中增加 `GateResults []GateResult` 供报告使用。
   - 修改 [engine.go](evaluation/workflow/promptiter/engine/engine.go) `executeRound`（L372-495），在调用 `accept` 前用 `CompareCaseDeltas` 比较当前 accepted baseline validation 与本轮 validation，并传入 `BudgetUsage`。
   - 新增一个通用 usage provider（例如 engine `Option`：`WithBudgetUsageProvider(func() BudgetUsage)`），由 example 汇总五个 FakeEngine 的 `CallCount()`、模拟成本和耗时后提供给 engine；engine 不直接依赖 FakeEngine。

   这样改动会覆盖约 4 个 engine 文件（`accept.go`、`engine.go`、新增 `case_delta.go`、如采用 option 则修改 `options.go`）及 example 侧的配置/报告文件，但不破坏现有单维用法（`NoNewHardFail=false` 且 `CriticalCaseIDs=nil` 且 `BudgetConstraint=nil` 时退化为原逻辑）。

**复用现有代码**：

- [accept.go](evaluation/workflow/promptiter/engine/accept.go) L13-16 `AcceptancePolicy`（扩展）、L21-37 `accept`（扩展）
- [engine.go L285-291](evaluation/workflow/promptiter/engine/engine.go) accept 调用点（注入 caseDeltas/cost/latency）
- [stop.go](evaluation/workflow/promptiter/engine/stop.go) StopPolicy（不受影响）

### 阶段 6：审计报告

**目标**：生成 `optimization_report.json` 和 `optimization_report.md`。

**步骤**：

1. 定义报告结构：

```go
// 示意代码
type OptimizationReport struct {
    Metadata ReportMetadata `json:"metadata"`
    Baseline BaselineSummary `json:"baseline"`
    Candidate CandidateSummary `json:"candidate"`
    Delta DeltaSummary `json:"delta"`
    GateDecision promptiterengine.AcceptanceDecision `json:"gate_decision"`
    FailureAttributionStats FailureAttributionStats `json:"failure_attribution_stats"`
    CostLatency CostLatencySummary `json:"cost_latency"`
    Rounds []RoundSnapshot `json:"rounds"`
}

type ReportMetadata struct {
    GeneratedAt      time.Time `json:"generated_at"`
    RandomSeed       int64     `json:"random_seed"`
    ModelConfig      string    `json:"model_config"`      // "fake_engine" 或真实模型配置
    FakeEngineConfig string    `json:"fake_engine_config"` // fake model 队列配置
    PipelineVersion  string    `json:"pipeline_version"`
}

type RoundSnapshot struct {
    RoundID           int                    `json:"round_id"`
    CandidatePrompt   string                 `json:"candidate_prompt"`
    PatchSet          []PatchEntry           `json:"patch_set"`
    TrainEvalResult   json.RawMessage        `json:"train_eval_result"`
    ValidationEvalResult json.RawMessage     `json:"validation_eval_result"`
    CaseDeltas        []promptiterengine.CaseDelta `json:"case_deltas"`
    AcceptanceDecision promptiterengine.AcceptanceDecision `json:"acceptance_decision"`
    RoundCost         float64                `json:"round_cost"`
    RoundLatency      time.Duration          `json:"round_latency"`
}
```

2. 实现 JSON 报告生成：

```go
// 示意代码
func (b *AuditReportBuilder) BuildJSON(
    baselineResult, candidateResult *promptiterengine.EvaluationResult,
    caseDeltas []promptiterengine.CaseDelta,
    gateDecision promptiterengine.AcceptanceDecision,
    attributions []FailureAttribution,
    roundResults []promptiterengine.RoundResult,
    metadata ReportMetadata,
) ([]byte, error) {
    report := OptimizationReport{
        Metadata: metadata,
        Baseline: summarizeBaseline(baselineResult),
        Candidate: summarizeCandidate(candidateResult),
        Delta: summarizeDeltas(caseDeltas),
        GateDecision: gateDecision,
        FailureAttributionStats: summarizeAttributions(attributions),
        CostLatency: summarizeCostLatency(metadata, roundResults),
        Rounds: collectRoundSnapshots(roundResults),
    }
    return json.MarshalIndent(report, "", "  ")
}
```

3. 实现 MD 报告生成（人读版）：

```go
// 示意代码
func (b *AuditReportBuilder) BuildMD(...) []byte {
    var md strings.Builder
    md.WriteString("# Optimization Report\n\n")
    md.WriteString(fmt.Sprintf("## 决策: %s\n\n", decisionEmoji(gateDecision.Accepted)))
    md.WriteString(fmt.Sprintf("- **接受**: %v\n", gateDecision.Accepted))
    md.WriteString(fmt.Sprintf("- **理由**: %s\n\n", gateDecision.Reason))
    md.WriteString("## Gate 详情\n\n")
    for _, r := range gateDecision.GateResults {
        md.WriteString(fmt.Sprintf("- %s: %s (%s)\n", r.GateName, passFail(r.Passed), r.Reason))
    }
    md.WriteString("\n## Baseline vs Candidate\n\n")
    md.WriteString("| Metric | Baseline | Candidate | Delta |\n|--------|----------|-----------|-------|\n")
    // ... 填充表格
    md.WriteString("\n## 逐 case delta\n\n")
    // ... 填充表格
    md.WriteString("\n## 失败归因统计\n\n")
    // ... 填充表格
    md.WriteString("\n## 成本与耗时\n\n")
    // ... 填充表格
    return []byte(md.String())
}
```

4. 写入文件：

```go
// 示意代码
os.WriteFile("report/optimization_report.json", jsonReport, 0644)
os.WriteFile("report/optimization_report.md", mdReport, 0644)
```

**报告必须包含的字段**（对照赛题要求）：

- baseline 分数 ✅ `Baseline.Summary`
- candidate 分数 ✅ `Candidate.Summary`
- 逐 case delta ✅ `Delta.CaseDeltas`
- gate 决策 ✅ `GateDecision.Accepted`
- 拒绝或接受理由 ✅ `GateDecision.Reason`
- 每轮候选 prompt ✅ `Rounds[].CandidatePrompt`
- eval result ✅ `Rounds[].TrainEvalResult/ValidationEvalResult`
- delta ✅ `Rounds[].CaseDeltas`
- 接受/拒绝理由 ✅ `Rounds[].AcceptanceDecision`
- 运行成本 ✅ `CostLatency.TotalCost`
- 耗时 ✅ `CostLatency.TotalLatency`
- 随机种子 ✅ `Metadata.RandomSeed`
- 模型配置或 fake engine 配置 ✅ `Metadata.ModelConfig/FakeEngineConfig`
- 失败归因统计 ✅ `FailureAttributionStats`

### 阶段 7：确定性保证

**目标**：确保无 API Key 时 pipeline 可重复运行，输出确定。

**步骤**：

1. **FakeEngine 包装 QueueModel**：解决 [QueueModel](test/mock_model.go) "每次回放整个队列"语义不符的问题（[mock_model.go L42-50](test/mock_model.go) `for _, call := range m.Calls` 遍历全部 Calls）。FakeEngine 按调用次数顺序消费预设响应：

```go
// 示意代码
type FakeEngine struct {
    mu            sync.Mutex
    name          string
    callIndex     int                  // 当前调用次数
    responseQueue [][]*model.Response  // 按调用次数顺序的响应队列
    callCount     int                  // 累计调用次数（供成本统计）
}

func (e *FakeEngine) GenerateContent(ctx context.Context, req *model.Request) (<-chan *model.Response, error) {
    e.mu.Lock()
    e.callCount++  // 累计调用次数，供 Gate 4 与报告使用
    if e.callIndex >= len(e.responseQueue) {
        e.mu.Unlock()
        return nil, fmt.Errorf("fake engine exhausted: call %d beyond queue %d", e.callIndex, len(e.responseQueue))
    }
    responses := e.responseQueue[e.callIndex]
    e.callIndex++
    e.mu.Unlock()

    ch := make(chan *model.Response, len(responses))
    go func() {
        defer close(ch)
        for _, resp := range responses {
            ch <- resp
        }
    }()
    return ch, nil
}

// CallCount 返回累计调用次数，供 Gate 4（BudgetConstraint）与报告的 CostLatency 使用。
func (e *FakeEngine) CallCount() int {
    e.mu.Lock()
    defer e.mu.Unlock()
    return e.callCount
}

// Info 使 FakeEngine 完整实现 model.Model。
func (e *FakeEngine) Info() model.Info {
    return model.Info{Name: e.name}
}
```

2. **多角色 FakeEngine 实例化**：PromptIter 的 backwarder / aggregator / optimizer / candidate / judge 五个角色请求/响应格式不同（backwarder 期望返回结构化 gradient JSON、aggregator 期望返回合并后的 gradient、optimizer 期望返回 patch JSON、candidate 返回普通文本响应、judge 返回 rubric 评分）。**为每个角色各建一个 FakeEngine 实例**，各自维护独立响应队列，避免格式混淆：

```go
// 示意代码（非 file 链接）
// fake_model_queue.json 按角色分段，加载时分别构造队列
candidateEngine  := fakeengine.New(candidateRespQueue)     // 普通文本响应
judgeEngine      := fakeengine.New(judgeRespQueue)         // rubric 评分 JSON
backwarderEngine := fakeengine.New(backwarderRespQueue)    // gradient JSON
aggregatorEngine := fakeengine.New(aggregatorRespQueue)    // 合并 gradient JSON
optimizerEngine  := fakeengine.New(optimizerRespQueue)     // patch JSON
```

各角色响应的 `*model.Response` 需按对应 schema 构造（参考 [backwarder](evaluation/workflow/promptiter/backwarder)、[aggregator](evaluation/workflow/promptiter/aggregator)、[optimizer](evaluation/workflow/promptiter/optimizer) 的反序列化结构）。建议先跑通一个最小 syncrun 变体（替换 openai 为 FakeEngine），验证五角色响应 schema 后再构造完整队列。

3. **成本/调用次数采集**（供 Gate 4 与报告 `CostLatency`）：

- **调用次数**：各 FakeEngine 实例的 `CallCount()` 之和。在 gate 评估前汇总：`totalCalls := candidateEngine.CallCount() + judgeEngine.CallCount() + backwarderEngine.CallCount() + aggregatorEngine.CallCount() + optimizerEngine.CallCount()`。
- **成本**：fake 模式下无真实 API 计费，`TotalCost` 设为 `0`（或按预设单价 × 调用次数模拟，如 `0.001 * float64(totalCalls)`）。
- **耗时**：在 `engine.Run` 前后用 `time.Since` 记录 `TotalLatency`。
- **gate 注入**：方式 B 下，example 通过 engine 的通用 usage provider 暴露当前累计调用次数/模拟成本；`executeRound` 用 provider 快照计算本轮 `BudgetUsage`，再传给 `accept`。外层编排层汇总全流程 cost/latency 写入报告。

4. **固定随机种子**：在 optimizer 采样、aggregator 聚合等涉及随机的环节固定种子。**注意**：fake 模式下 backwarder/aggregator/optimizer 通过 FakeEngine 返回预设响应，不涉及采样随机性，`rand.Seed` 实际无作用；仅真实模型模式下采样才依赖 rand。保留 `rand.Seed` 仅为真实模型模式预留：

```go
// 示意代码
// fake 模式：确定性由 FakeEngine 的队列顺序保证，rand.Seed 无实际作用
// 真实模型模式：rand.Seed 固定采样随机性
rand.Seed(metadata.RandomSeed)  // 或用 math/rand/v2 的 New(rand.NewSource(seed))
```

5. **禁用真实 API Key**：全程使用 FakeEngine + EvalModeTrace，不依赖任何外部 API

6. **trace mode 优化**：对于 baseline 评测和候选验证，若 evalset 已含 trace（`EvalModeTrace`，[evalcase.go L19-26](evaluation/evalset/evalcase.go)），直接用 trace 中的 actual conversation，跳过真实推理，加速 pipeline

**耗时控制**（满足验收标准 5：≤ 3 分钟）：

- 使用 FakeEngine 替代真实 LLM 调用，每次调用 < 1ms
- 使用 EvalModeTrace 跳过真实推理
- 限制 MaxRounds（如 5 轮）
- 限制 case 数量（6 条）
- 预估总耗时：6 cases × 5 rounds × (1ms + eval) << 3 分钟

### 阶段 8：测试覆盖

**目标**：单元测试覆盖 gate 决策、逐 case delta、失败归因、报告生成。

**测试用例设计**：

1. **gate 决策测试**（`gate_test.go`）：
   - 所有 gate 通过 → Accepted
   - ValidationScoreGain 不足 → Rejected
   - NoNewHardFail 触发（新增失败 case）→ Rejected
   - CriticalCasePreserve 触发（关键 case 退化）→ Rejected
   - BudgetConstraint 触发（成本超预算）→ Rejected
   - 多个 gate 同时失败的组合

2. **逐 case delta 测试**（`case_delta_test.go`）：
   - NewlyPassed（baseline fail → candidate pass）
   - NewlyFailed（baseline pass → candidate fail）
   - ScoreImproved（分数提升但状态不变）
   - ScoreRegressed（分数下降但状态不变）
   - Unchanged（无变化）

3. **失败归因测试**（`failure_attribution_test.go`）：
   - 最终回复不匹配（ROUGE fail）
   - 工具调用错误（ToolTrajectory Name 不匹配）
   - 工具参数错误（ToolTrajectory Arguments 不匹配）
   - route 错误（ExecutionTrace transfer 异常）
   - 格式错误（LLM Rubric 格式类 rubric fail）
   - 知识召回不足（LLM Rubric 知识类 rubric fail）

4. **报告生成测试**（`audit_report_test.go`）：
   - JSON 报告字段完整性（所有必填字段存在）
   - MD 报告可读性（含决策、gate 详情、表格）
   - 随机种子和模型配置正确记录

5. **集成测试**（`main_test.go`）：
   - 6 条样例 case 全跑通
   - fake model/trace mode 下完整 pipeline 耗时 ≤ 3 分钟
   - 生成 `optimization_report.json` 和 `optimization_report.md`
   - 过拟合场景（验证集退化但训练集提升）被正确拒绝

---

## 第 4 章 验收标准对齐

逐条映射赛题验收标准到实现阶段：

### 验收标准 1：6 条样例 case 全部可运行，生成完整优化报告

**对应阶段**：阶段 0（样例准备）+ 阶段 1-7（完整 pipeline）+ 阶段 6（报告生成）

**验证方式**：运行 `go run main.go`，检查 `report/optimization_report.{json,md}` 生成且字段完整。

### 验收标准 2：隐藏样本上优化接受/拒绝决策准确率 ≥ 80%

**对应阶段**：阶段 5（多维 gate）+ 阶段 8（测试覆盖）

**实现要点**：
- Gate 配置可调（阈值、关键 case 列表、预算）
- 测试集覆盖各类接受/拒绝场景
- 决策基于多维信号（总分 + hard fail + critical case + 预算），降低误判

### 验收标准 3：对"验证集退化但训练集提升"的过拟合场景必须拒绝

**对应阶段**：阶段 5 的 Gate 1（ValidationScoreGain，第一道防线）+ Gate 2（NoNewHardFail）+ Gate 3（CriticalCasePreserve）

**实现要点**：
- validation.evalset.json 中包含"验证集退化"case
- CaseDeltaCalculator 识别 `NewlyFailed` 和 `ScoreRegressed`
- **分层防御**：
  - **Gate 1（总分，第一道防线）**：验证集总分退化时 `scoreDelta` 为负，直接被 `MinScoreGain` 阈值拒绝——这是"验证集退化但训练集提升"场景的主要拦截器；
  - **Gate 2/3（分布，第二道防线）**：当验证集总分微升但个别 case 退化（新增 fail 或关键 case 分数下降）时，由 NoNewHardFail 与 CriticalCasePreserve 拦截"总分微升但分布退化"的隐蔽过拟合。
- 三层 gate 任一触发即拒绝候选，覆盖显性与隐性过拟合

**关键代码路径**：
- 阶段 4 `CaseDeltaCalculator.Compare` 识别 `CaseDeltaNewlyFailed` 和 `CaseDeltaScoreRegressed`
- 阶段 5 `accept`（方式 B）的 Gate 1（scoreDelta < MinScoreGain）/ Gate 2（NewlyFailed）/ Gate 3（critical regressed）分层检查

### 验收标准 4：失败归因分类准确率 ≥ 75%，每个失败 case 至少给出一个可解释原因

**对应阶段**：阶段 2（FailureAttributor）

**实现要点**：
- 六类语义归类基于 metric 类型 + rubric 内容 + tool trajectory + ExecutionTrace
- 每个 fail case 至少触发一个归因（因为每个 fail case 至少有一个 metric fail）
- Evidence 字段提供具体证据（不匹配的工具名、缺失的 schema 字段等）

### 验收标准 5：fake model/trace mode 下完整 pipeline 耗时 ≤ 3 分钟

**对应阶段**：阶段 7（确定性保证）

**实现要点**：
- FakeEngine 每次调用 < 1ms
- EvalModeTrace 跳过真实推理
- 6 cases × 5 rounds × (1ms + eval) << 3 分钟
- 限制 MaxRounds 和 case 数量

### 验收标准 6：报告必须包含 baseline 分数、candidate 分数、逐 case delta、gate 决策、拒绝或接受理由

**对应阶段**：阶段 6（AuditReportBuilder）

**实现要点**：
- `OptimizationReport.Baseline.Summary` —— baseline 分数
- `OptimizationReport.Candidate.Summary` —— candidate 分数
- `OptimizationReport.Delta.CaseDeltas` —— 逐 case delta
- `OptimizationReport.GateDecision.Accepted` —— gate 决策
- `OptimizationReport.GateDecision.Reason` —— 拒绝或接受理由

---

## 第 5 章 方案设计说明（300-500 字）

本方案构建"评测 - 失败归因 - prompt 优化 - 回归验证 - 产物审计"的自动闭环，最大化复用 trpc-agent-go 现有能力，最小化新增代码。

**失败归因方法**：基于 PromptIter engine 的 `TerminalLoss` 提取机制（[loss.go](evaluation/workflow/promptiter/engine/loss.go) L30-82），扩展为六类语义归类：最终回复不匹配（ROUGE/FinalResponse fail）、工具调用错误（ToolTrajectory Name 不匹配）、工具参数错误（Arguments 子准则 fail）、route 错误（ExecutionTrace transfer 异常）、格式错误（LLM Rubric 格式类 rubric fail）、知识召回不足（LLM Rubric 知识类 rubric fail）。归因组件直接消费 `engine.EvaluationResult`（遍历 `EvalSets[].Cases[]`，见 [2.3 类型约定](#23-类型约定重要)），每个失败 case 至少触发一个归因，Evidence 字段提供具体证据。

**接受策略**：采用方式 B 扩展 [accept.go](evaluation/workflow/promptiter/engine/accept.go) 的 `AcceptancePolicy` 与 `accept` 方法，实现四维 gate：验证集总分提升 ≥ 阈值（Gate 1，保留原 MinScoreGain 语义）、不新增 hard fail（Gate 2，检测 `CaseDeltaNewlyFailed`）、关键 case 不得退化（Gate 3，检测指定 case 的 `ScoreRegressed`）、成本/调用次数 ≤ 预算（Gate 4，调用次数由 FakeEngine.CallCount 采集）。engine 内外用同一套 gate，保证状态一致，全部通过才接受候选。

**防过拟合策略**：训练集与验证集严格分离；候选验证直接复用 `runResult.Rounds[].Validation`（公共 API 无法注入 profile，见阶段 4）；CaseDeltaCalculator 逐 case 比较 baseline vs candidate；**分层防御**——Gate 1 拦截验证集总分退化（过拟合主场景），Gate 2/3 拦截总分微升但分布退化的隐蔽过拟合。

**PromptIter 接入方式**：复用 [engine.Run](evaluation/workflow/promptiter/engine/engine.go) L208-329 的多轮优化主循环，直接取用 `runResult.Rounds`（已含每轮完整 RoundResult，无需 Observer 收集）。外层 RegressionLoop 编排 pipeline，FakeEngine 为 candidate/judge/backwarder/aggregator/optimizer 五角色各建独立实例，按调用次数顺序消费预设响应。

**产物审计方式**：AuditReportBuilder 生成 JSON + MD 双格式报告。JSON 含 metadata（随机种子、模型配置、fake engine 配置）、baseline/candidate 摘要、逐 case delta、gate 决策、失败归因统计、成本耗时、每轮快照。MD 是人读版，含决策结论、gate 详情表格、baseline vs candidate 对比。所有字段对照赛题验收标准 6 设计。
