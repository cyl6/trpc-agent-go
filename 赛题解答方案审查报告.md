# 赛题解答方案审查报告

> 审查对象：[赛题解答方案.md](赛题解答方案.md)
> 审查依据：[赛题.md](赛题.md) 及 trpc-agent-go 实际代码库
> 审查日期：2026-06-27
> 审查方法：逐条核对方案引用的代码位置、API 签名、类型定义与数据流，对照赛题六阶段与六项验收标准

---

## 一、审查概述

[赛题解答方案.md](赛题解答方案.md) 围绕 [赛题.md](赛题.md) 的"评测 - 失败归因 - prompt 优化 - 回归验证 - 产物审计"自动闭环，给出了三层架构（编排层 / 新增层 / 复用层）、八个实现阶段、五个 Gap 分析与四维 gate 设计。整体方向正确，Gap 识别大体准确，对现有能力的复用思路合理。

但经逐文件核对源码，方案在**类型系统、API 签名、数据流可行性**三个层面存在多处与实际代码不符的硬伤，若不修正将直接导致代码无法编译或 pipeline 无法跑通。同时方案对部分现有能力的描述偏弱（如 manager 的存储能力被低估），对若干关键技术难点（profile 注入、EvaluationResult 类型转换、FakeEngine 多角色复用）缺乏可行解。

**总体结论：方案架构思路可行，但当前版本不能直接据此完成赛题。需在修正 6 处必修问题、补充 4 处关键技术细节后方可进入实现阶段（详见第六、七章）。**

---

## 二、方案优势

### 2.1 Gap 识别大体准确

方案的 [1.3 关键 Gap](赛题解答方案.md) 识别出的五个 Gap 与源码核对结果基本一致：

| Gap | 方案描述 | 源码核对结果 |
|-----|---------|------------|
| Gap 1 失败归因缺失语义归类 | [loss.go](evaluation/workflow/promptiter/engine/loss.go) `loss` 函数只提取 TerminalLoss | ✅ 准确，L30-82 确实只提取 `MetricName/Severity/StepID/Loss`，未做六类语义归类 |
| Gap 2 逐 case delta 缺失 | [accept.go](evaluation/workflow/promptiter/engine/accept.go) L33-37 只比较总分 | ✅ 准确，L33-37 为 `scoreDelta := candidateScore - baselineScore; Accepted: scoreDelta >= policy.MinScoreGain` |
| Gap 3 多维 gate 缺失 | `AcceptancePolicy` 只有 `MinScoreGain` 单字段 | ✅ 准确，[accept.go](evaluation/workflow/promptiter/engine/accept.go) L13-16 仅一字段 |
| Gap 4 审计报告缺失 | manager 未生成 optimization_report.json | ⚠️ 部分准确（见 §4.5） |
| Gap 5 QueueModel 语义不符 | 每次调用回放整个队列 | ✅ 准确，[mock_model.go](test/mock_model.go) L42-50 `for _, call := range m.Calls` 确实遍历全部 Calls |

### 2.2 关键代码位置引用精度较高

方案对 [loss.go](evaluation/workflow/promptiter/engine/loss.go) `traceTerminalStepIDs`（L207-254）、[evalcase.go](evaluation/evalset/evalcase.go) `EvalModeTrace`（L19-26）、[tooltrajectory.go](evaluation/metric/criterion/tooltrajectory/tooltrajectory.go) `matchTool`（L150-166）的位置引用基本准确，便于后续实现时定位。

### 2.3 架构分层与复用思路合理

三层架构（[第 2 章](赛题解答方案.md)）将新增组件（FailureAttributor / CaseDeltaCalculator / MultiDimensionGate / AuditReportBuilder / FakeEngine）与现有 evaluation service / PromptIter engine 解耦，符合"最大化复用、最小化新增"的原则。推荐"方式 A（外层包装）"集成 gate 以避免修改 engine 代码，方向正确。

### 2.4 四维 gate 设计直接映射赛题要求

[阶段 5](赛题解答方案.md) 的四类 gate（ValidationScoreGain / NoNewHardFail / CriticalCasePreserve / BudgetConstraint）与 [赛题.md](赛题.md) 第 16 行"验证集总分提升 ≥ 阈值/不能新增 hard fail/关键 case 不能退化/成本或调用次数不能超预算"一一对应，覆盖完整。

### 2.5 失败归因六类与赛题要求一致

[阶段 2](赛题解答方案.md) 的六类 FailureCategory（最终回复不匹配 / 工具调用错误 / 工具参数错误 / route 错误 / 格式错误 / 知识召回不足）与 [赛题.md](赛题.md) 第 14 行的六类完全对应。

### 2.6 测试覆盖计划完备

[阶段 8](赛题解答方案.md) 针对 gate / case delta / 失败归因 / 报告生成 / 集成测试五类设计了具体用例，覆盖赛题交付物"单元测试覆盖 gate 决策、逐 case delta、失败归因、报告生成"的要求。

---

## 三、方案不足：必修级技术问题

以下 6 个问题会导致代码无法编译或 pipeline 无法跑通，**必须在实现前修正**。

### 3.1 【必修】存在 EvaluationResult 类型混淆，代码无法编译

**问题**：方案的 `FailureAttributor.Attribute`（[方案 L307-361](赛题解答方案.md)）与 `CaseDeltaCalculator.Compare`（[方案 L487-523](赛题解答方案.md)）函数签名声明使用 `*evaluation.EvaluationResult`，但函数体内访问的字段不属于该类型。

**源码证据**：

- `evaluation.EvaluationResult`（[evaluation.go L146-153](evaluation/evaluation.go)）的字段为：
  ```go
  type EvaluationResult struct {
      AppName       string
      EvalSetID     string
      OverallStatus status.EvalStatus
      ExecutionTime time.Duration
      EvalCases     []*EvaluationCaseResult   // ← 元素是 evaluation.EvaluationCaseResult
      EvalResult    *evalresult.EvalSetResult
  }
  ```
- `evaluation.EvaluationCaseResult`（[evaluation.go L156-162](evaluation/evaluation.go)）的字段为 `OverallStatus` 与 `MetricResults`，**没有** `FinalEvalStatus` 与 `OverallEvalMetricResults`。
- 方案代码访问的 `caseResult.FinalEvalStatus` 与 `caseResult.OverallEvalMetricResults` 实际属于 `evalresult.EvalCaseResult`（[evalresult.go L39-58](evaluation/evalresult/evalresult.go)），这是**另一个类型**。

**后果**：方案 L313 `caseResult.FinalEvalStatus == evalstatus.Passed`、L316 `caseResult.OverallEvalMetricResults`、L504 `baseCase.FinalEvalStatus`、L505 `averageMetricScore(baseCase)` 等均无法通过编译。

**补充**：方案使用的 `evalstatus.Passed` / `evalstatus.Failed` 包名也不准确，实际包为 `status`（[evaluation.go L36](evaluation/evaluation.go) `"trpc.group/trpc-go/trpc-agent-go/evaluation/status"`），常量为 `status.EvalStatusPassed` / `status.EvalStatusFailed`。

**更深层问题**：engine 包还有**第三个** `EvaluationResult`（[evaluate.go L93-98](evaluation/workflow/promptiter/engine/evaluate.go)）：
```go
type EvaluationResult struct {
    OverallScore float64
    EvalSets     []EvalSetResult
}
```
`RunResult.Rounds[].Train` 与 `.Validation`（[engine.go L136/L148](evaluation/workflow/promptiter/engine/engine.go)）使用的正是这个 engine 包的类型，而**不是** `evaluation.EvaluationResult`。方案完全未提及这一类型差异，导致"从 engine 收集 RoundResult 后做失败归因与 case delta"的数据通路存在类型断层。

**建议**：明确选择以下二选一并贯穿全方案：
- 方案 A：FailureAttributor / CaseDeltaCalculator 直接消费 `engine.EvaluationResult`（`EvalSets[].Cases[]`），与 loss.go 的访问方式保持一致；
- 方案 B：在编排层做 `engine.EvaluationResult` → `evaluation.EvaluationResult` 的适配转换，再交给归因/delta 组件。

### 3.2 【必修】候选 prompt 无法通过公共 API 重新跑验证集

**问题**：[阶段 4 步骤 1](赛题解答方案.md)（方案 L454-461）声称：
```go
candidateValidationResult, err := evaluator.Evaluate(
    ctx, validationEvalSetID,
    evaluation.WithRunOptions(/* 注入 acceptedProfile */),
)
```
但 `evaluation.AgentEvaluator.Evaluate`（[evaluation.go L186](evaluation/evaluation.go)）的 `Option` 列表里**没有**注入 profile 的能力。

**源码证据**：
- [evaluation/options.go L212](evaluation/options.go) 的 `WithRunOptions(opt ...agent.RunOption) Option` 接收的是 `agent.RunOption`（agent 运行时选项，如超时、回调），**不是** profile 注入；
- profile 的应用是 PromptIter engine **内部**行为：`applyPatchSet`（[engine.go L461](evaluation/workflow/promptiter/engine/engine.go)）生成 `outputProfile`，再由 `e.evaluate(ctx, structure, e.newEvaluationRequest(..., outputProfile, ...))`（[engine.go L469-475](evaluation/workflow/promptiter/engine/engine.go)）通过 `EvaluationRequest.Profile`（[evaluate.go L47](evaluation/workflow/promptiter/engine/evaluate.go)）传入，而 `engine.evaluate` 是**非导出方法**。

**后果**：方案强调"用 acceptedProfile 重跑 validation evalset（确保独立验证，不依赖 engine 内部的 validation 评估）"这一独立验证步骤**无法用公共 API 实现**，而赛题验收标准 1（6 条 case 全跑通）依赖该步骤。

**建议**：放弃"独立重跑"思路，改为直接复用 engine 最后一轮的 `RoundResult.Validation`（已是 acceptedProfile 或 candidateProfile 的验证结果）。若坚持独立验证，需在编排层自行实现 profile → agent.RunOption 的桥接（即把 profile 的 surface 文本回写到 agent 配置后再 Evaluate），但这与 PromptIter 的 profile 语义不对等，复杂度高且易出错。

### 3.3 【必修】engine.New 缺少 ctx 参数

**问题**：[阶段 3 步骤 1](赛题解答方案.md)（方案 L387-393）的 `promptiterengine.New` 调用缺少第一个参数 `ctx context.Context`。

**源码证据**：[engine.go L170-175](evaluation/workflow/promptiter/engine/engine.go) 实际签名：
```go
func New(ctx context.Context,
    targetAgent agent.Agent,
    agentEvaluator evaluation.AgentEvaluator,
    backwarder backwarder.Backwarder,
    aggregator aggregator.Aggregator,
    optimizer optimizer.Optimizer) (Engine, error)
```
syncrun 示例 [engine.go L195-202](examples/evaluation/promptiter/syncrun/engine.go) 也确认首参为 `ctx`。

**后果**：编译失败。

### 3.4 【必修】RunRequest 构造存在 4 处字段错误

**问题**：[阶段 3 步骤 2](赛题解答方案.md)（方案 L399-414）的 `RunRequest` 构造与实际类型不符。

**源码证据**（对照 [engine.go L40-67](evaluation/workflow/promptiter/engine/engine.go) 与 syncrun [engine.go L219-256](examples/evaluation/promptiter/syncrun/engine.go)）：

| 方案写法 | 实际类型 | 问题 |
|---------|---------|------|
| `Train: trainEvalSetInput` | `Train []EvalSetInput` | 应为 `Train: []EvalSetInput{trainEvalSetInput}` |
| `EvaluationOptions: []evaluation.EvaluateOption{...}` | `EvaluationOptions EvaluationOptions`（结构体） | 应为 `EvaluationOptions: promptiterengine.EvaluationOptions{...}`，且类型不属于 `evaluation` 包 |
| `StopPolicy{ TargetScore: 0.9 }` | `StopPolicy.TargetScore *float64`（指针） | 应为 `targetScore := 0.9; StopPolicy{ TargetScore: &targetScore }` |
| `TargetSurfaceIDs: []string{"Instruction"}` | `[]string`，但元素需为 `astructure.SurfaceID(nodeID, surfaceType)` 计算结果 | `"Instruction"` 不是合法 surface ID |

**关于 TargetSurfaceIDs 的额外说明**：[structure.go L46-52](agent/structure/structure.go) 定义 `SurfaceTypeInstruction = "instruction"`（小写），而 [structure.go L90-91](agent/structure/structure.go) 的 `SurfaceID(nodeID, surfaceType)` 生成的是 `nodeID + "#" + surfaceType` 形式（如 `"agentName#instruction"`）。syncrun 示例 [engine.go L108](examples/evaluation/promptiter/syncrun/engine.go) 正是 `astructure.SurfaceID(candidateAgentName, astructure.SurfaceTypeInstruction)`。方案 L412 直接写字面量 `"Instruction"` 会被 [handler.go L250-256](server/promptiter/handler.go) 的 `validateTargetSurfaceIDs` 判为 unknown。

**后果**：编译失败或运行时校验报错。

### 3.5 【必修】Observer 收集 RoundResult 的方式错误

**问题**：[阶段 3 步骤 3](赛题解答方案.md)（方案 L419-425）的 Observer 代码：
```go
observer := func(ctx context.Context, event *promptiterengine.Event) error {
    if event.Kind == promptiterengine.EventKindRoundCompleted {
        roundResults = append(roundResults, event.Round)  // ← event.Round 是 int
    }
    return nil
}
```
`event.Round` 是 `int`（轮次序号，[event.go L50](evaluation/workflow/promptiter/engine/event.go)），而 `roundResults` 声明为 `[]promptiterengine.RoundResult`，类型不匹配，无法编译。

**源码证据**：
- [event.go L46-53](evaluation/workflow/promptiter/engine/event.go) 的 `Event.Payload` 为 `any`；
- `EventKindRoundCompleted` 的 payload 类型是 `*RoundCompleted`（[observer.go L161](evaluation/workflow/promptiter/manager/observer.go)），仅含 `Accepted/AcceptanceReason/ScoreDelta/ShouldStop/StopReason` 摘要字段（[event.go L59-70](evaluation/workflow/promptiter/engine/event.go)），**不含**完整 `RoundResult`。

**更关键**：`engine.Run` 返回的 `*RunResult` 已自带 `Rounds []RoundResult`（[engine.go L124](evaluation/workflow/promptiter/engine/engine.go)），每轮的 Train/Validation/Losses/Patches/OutputProfile/Acceptance 全部已填充（见 [engine.go L321](evaluation/workflow/promptiter/engine/engine.go) `result.Rounds = append(result.Rounds, *roundResult)`）。**完全无需用 Observer 收集**。

**建议**：删除 Observer 收集逻辑，直接 `runResult.Rounds` 取用。

### 3.6 【必修】"方式 A 包装"gate 集成存在状态不一致风险

**问题**：[阶段 5 步骤 3](赛题解答方案.md)（方案 L651-656）推荐"方式 A（包装）"：engine 内部仍用单维 gate，外部 MultiDimensionGate 做最终决策。但未处理两者判定不一致时的状态问题。

**源码证据**：[engine.go L285-291](evaluation/workflow/promptiter/engine/engine.go) 中，一旦 `roundResult.Acceptance.Accepted` 为 true，engine 立即更新 `acceptedProfile = roundResult.OutputProfile` 与 `acceptedValidationScore = effectiveScore`。若外层 MultiDimensionGate 随后拒绝该候选，engine 的 `RunResult.AcceptedProfile` 已指向被拒绝的 profile，后续轮次会基于该被拒绝 profile 继续优化，造成"外层拒绝但 engine 已采纳"的状态分裂。

**后果**：验收标准 3（过拟合场景必须拒绝）可能误判——engine 内部已接受并推进，外层 gate 的拒绝无法回滚 engine 状态。

**建议**：采用"方式 B（替换）"——扩展 `AcceptancePolicy` 结构体增加多维字段，让 engine 内外用同一套 gate。虽需修改 [accept.go](evaluation/workflow/promptiter/engine/accept.go)，但能保证状态一致。修改量可控（accept.go 仅 44 行）。

---

## 四、方案不足：应修级技术问题

以下问题不会直接导致编译失败，但影响方案完备性与准确性。

### 4.1 manager 存储能力被低估

**方案描述**（[L29](赛题解答方案.md) 与 [L76](赛题解答方案.md)）：manager "只存 run 状态（Running/Succeeded/Failed/Canceled），未生成 optimization_report.json"。

**源码证据**：[manager/observer.go L41-179](evaluation/workflow/promptiter/manager/observer.go) 的 `applyEvent` 对每种事件（StructureSnapshot / BaselineValidation / RoundTrainEvaluation / RoundLosses / RoundBackward / RoundAggregation / RoundPatchSet / RoundOutputProfile / RoundValidation / RoundCompleted）都做了完整持久化，每轮的 Train/Validation/Losses/Patches/OutputProfile/Acceptance 全部写入 store（[observer.go L38](evaluation/workflow/promptiter/manager/observer.go) `o.manager.store.Update`）。

**结论**：manager 存的不是"仅 run 状态"，而是**完整的 RunResult 快照流**。方案的核心论点（需新增 AuditReportBuilder 生成 optimization_report.json/md）仍成立，但对现状的描述不精确，会误导实现者低估可复用数据。建议改为"manager 已持久化完整 RunResult，但未按赛题要求的 optimization_report.json/md 格式与字段输出"。

### 4.2 baseline Profile 与 targetAgent 的构造缺失

**问题**：方案 [阶段 3](赛题解答方案.md) 提到 `InitialProfile: baselineProfile`（"从 baseline_prompt.txt 构造"），但未说明：
1. 如何把 `baseline_prompt.txt` 的文本构造成 `*promptiter.Profile`（Profile 结构包含 SurfaceEntries，需指定 NodeID/SurfaceType/Value）；
2. `engine.New` 必需的 `targetAgent agent.Agent` 如何构造——该 agent 需携带 baseline prompt 作为其 instruction surface，syncrun 示例中通过 `newCandidateAgent(candidateModel, cfg.CandidateInstruction)`（[engine.go L132](examples/evaluation/promptiter/syncrun/engine.go)）构造，方案未给出对应实现。

**后果**：实现者需自行摸索 Profile 与 Agent 的构造方式，可能踩坑。

### 4.3 FakeEngine 多角色复用方案不完整

**问题**：[阶段 0 步骤 6](赛题解答方案.md) 提到 fake_model_queue.json 需预设"candidate model / judge model / worker model（backward/aggregation/optimization）"的响应，但 [阶段 7](赛题解答方案.md) 的 FakeEngine 实现（方案 L793-817）只展示了一个通用按调用次数消费的队列，未说明：
1. 一个 FakeEngine 实例如何区分服务于 candidate / judge / backwarder / aggregator / optimizer 五种角色（它们请求/响应格式不同）；
2. backwarder 期望返回结构化 gradient、aggregator 期望返回合并后的 gradient、optimizer 期望返回 patch——这些都不是普通文本响应，需按各角色 schema 构造 `*model.Response`。

**后果**：fake_model_queue.json 的设计与 FakeEngine 的实现细节不清晰，是赛题验收标准 5（fake model 下跑通）的关键风险点。

### 4.4 成本/调用次数采集机制缺失

**问题**：[阶段 5](赛题解答方案.md) 的 Gate 4（BudgetConstraint）需要 `cost` 与 `apiCalls`，[阶段 6](赛题解答方案.md) 的报告也需要 `CostLatency.TotalCost`，但方案未说明在使用 FakeEngine（无真实 API 调用）时如何采集这两个值。

**源码证据**：`evaluation.AgentEvaluator` 与 `engine.Engine` 均未导出调用次数/成本统计接口。FakeEngine 自身可统计 `callIndex`（方案 L795），但该计数仅对应 FakeEngine 实例的调用，需明确"成本"在 fake 模式下的定义（如 0 成本 + 按 callIndex 计调用次数）。

**建议**：在 FakeEngine 中暴露 `CallCount()` 方法，gate 与报告据此计算；cost 在 fake 模式下设为 0 或按预设单价 × 调用次数模拟。

### 4.5 过拟合场景的 gate 触发逻辑表述不精确

**问题**：[阶段 5](赛题解答方案.md) 与 [验收标准 3 对齐](赛题解答方案.md)（方案 L900-908）声称过拟合场景由"Gate 2（NoNewHardFail）+ Gate 3（CriticalCasePreserve）"联合判定。但"验证集退化但训练集提升"的典型表现是**验证集总分下降**，未必有单 case 新增 fail。

**分析**：若验证集总分下降，Gate 1（ValidationScoreGain：`candidateScore - baselineScore >= threshold`）会因 scoreDelta 为负而直接拒绝。Gate 2/3 仅在"总分微升但个别 case 退化"时才起作用。方案将过拟合防御主要归功于 Gate 2/3，未点明 Gate 1 才是第一道防线，表述不够精确。

**建议**：明确"Gate 1 拦截总分退化，Gate 2/3 拦截总分微升但分布退化"的分层防御关系。

### 4.6 随机种子固定策略有效性存疑

**问题**：[阶段 7 步骤 2](赛题解答方案.md)（方案 L823-825）用 `rand.Seed(metadata.RandomSeed)` 固定随机性。但 PromptIter 的 backwarder/aggregator/optimizer 在 fake 模式下通过 FakeEngine 返回**预设响应**，不涉及采样随机性；engine 内部的 `runIndexedParallel`（[engine.go L590](evaluation/workflow/promptiter/engine/engine.go)）也不依赖 rand。`rand.Seed` 仅在真实 LLM 采样时有意义，fake 模式下实际无作用。

**建议**：明确"fake 模式下确定性由 FakeEngine 的队列顺序保证，rand.Seed 仅为真实模型模式预留"。

### 4.7 validateTargetSurfaceIDs 行号引用偏差

**问题**：[方案 L439](赛题解答方案.md) 称 `validateTargetSurfaceIDs` 位于 [server/promptiter/handler.go](server/promptiter/handler.go) "约 L261-271"。

**源码证据**：`validateTargetSurfaceIDs` 实际在 [handler.go L229-259](server/promptiter/handler.go)，L261-271 是辅助函数 `isSupportedTargetSurfaceType`。属轻微偏差，但影响实现者定位。

---

## 五、潜在风险与应对

| 风险 | 等级 | 应对措施 |
|------|------|---------|
| EvaluationResult 三类型混淆导致全链路数据流断裂 | 高 | 按 §3.1 建议统一类型选择，并在编排层写适配函数 |
| 公共 API 无法注入 profile，独立验证不可行 | 高 | 按 §3.2 改为复用 `RunResult.Rounds[len-1].Validation` |
| FakeEngine 多角色响应构造复杂，fake_model_queue.json 设计失误将致 pipeline 卡死 | 高 | 实现前先跑通一个最小 syncrun 变体（替换 openai 为 FakeEngine），验证五角色响应 schema |
| 方式 A 包装 gate 导致 engine 内外状态分裂 | 中 | 按 §3.6 改用方式 B（扩展 AcceptancePolicy） |
| 6 条样例 case 的"可优化成功/优化无效/验证集退化"三态需精确设计 fake 响应队列 | 中 | 阶段 0 优先设计 fake_model_queue.json 的"剧情脚本"，确保三类结果可复现 |
| 隐藏样本上 gate 决策准确率 ≥ 80%（验收标准 2）难以自验 | 中 | 在测试集中额外构造 ≥ 10 条覆盖各 gate 组合的 case 做离线验证 |
| 耗时 ≤ 3 分钟（验收标准 5）依赖 FakeEngine 调用次数可控 | 低 | FakeEngine 单次 < 1ms，6 cases × 5 rounds × ~20 调用/轮 ≈ 600ms，余量充足 |

---

## 六、改进建议（优先级排序）

### 建议 1（必修）：统一 EvaluationResult 类型选择

在方案 [第 2 章数据流](赛题解答方案.md) 开头增加一节"类型约定"，明确：
- engine 产出的 `RunResult.Rounds[].Train/Validation` 为 `engine.EvaluationResult`；
- FailureAttributor / CaseDeltaCalculator 直接消费 `engine.EvaluationResult`，遍历 `EvalSets[].Cases[]`；
- 字段映射：`case.OverallStatus`（非 FinalEvalStatus）、`case.Metrics[]`（非 OverallEvalMetricResults）、`metric.Status`（非 EvalStatus）、`metric.Reason`（非 Details.Reason）。

### 建议 2（必修）：删除"独立重跑验证集"，改为复用 engine 验证结果

[阶段 4 步骤 1](赛题解答方案.md) 改为：
```go
// 直接取 engine 最后一轮的 validation 结果（已是 candidate profile 的验证）
lastRound := runResult.Rounds[len(runResult.Rounds)-1]
candidateValidationResult := lastRound.Validation
baselineValidationResult := runResult.BaselineValidation
caseDeltas := CaseDeltaCalculator.Compare(baselineValidationResult, candidateValidationResult)
```
并注明 `BaselineValidation` 与 `RoundResult.Validation` 均为 `engine.EvaluationResult`。

### 建议 3（必修）：修正 engine.New 与 RunRequest 构造

按 §3.3、§3.4 修正 `engine.New` 首参为 `ctx`，修正 `Train`/`EvaluationOptions`/`StopPolicy.TargetScore`/`TargetSurfaceIDs` 四处字段。`TargetSurfaceIDs` 必须用 `astructure.SurfaceID(agentName, astructure.SurfaceTypeInstruction)` 计算。

### 建议 4（必修）：删除 Observer 收集，直接用 runResult.Rounds

[阶段 3 步骤 3](赛题解答方案.md) 整段替换为：
```go
runResult, err := engine.Run(ctx, request)
// runResult.Rounds 已含每轮完整 RoundResult
// runResult.AcceptedProfile 为最终接受 profile（若有）
```

### 建议 5（必修）：gate 集成改用方式 B

[阶段 5 步骤 3](赛题解答方案.md) 改为推荐方式 B，给出 `AcceptancePolicy` 扩展定义：
```go
type AcceptancePolicy struct {
    MinScoreGain        float64       // 保留原字段
    NoNewHardFail       bool
    CriticalCaseIDs     []string
    BudgetConstraint    *Budget
}
```
并相应修改 [accept.go](evaluation/workflow/promptiter/engine/accept.go) 的 `accept` 方法签名，注入 caseDeltas 与 cost/latency。需在方案中声明此为对现有代码的修改点。

### 建议 6（应修）：补充 Profile 与 targetAgent 构造说明

在 [阶段 3](赛题解答方案.md) 增加"Profile 构造"小节，说明：
- 如何从 baseline_prompt.txt 构造 `promptiter.Profile`（参考 `iprofile.Clone` 与 syncrun 的 agent 构造）；
- 如何构造 `targetAgent agent.Agent` 使其 instruction surface 携带 baseline prompt。

### 建议 7（应修）：细化 FakeEngine 多角色设计

在 [阶段 7](赛题解答方案.md) 增加"FakeEngine 多角色复用"小节，说明：
- 是否为 candidate/judge/backwarder/aggregator/optimizer 各建一个 FakeEngine 实例（推荐）；
- 各角色响应的 `*model.Response` 如何按 schema 构造（backwarder 返回 gradient JSON、optimizer 返回 patch JSON 等）；
- fake_model_queue.json 的组织方式（按角色分文件 or 单文件分段）。

### 建议 8（应修）：修正 manager 能力描述与过拟合 gate 逻辑表述

按 §4.1、§4.5 修正方案 L29、L76、L900-908 的表述。

---

## 七、验收标准达成度评估

对照 [赛题.md](赛题.md) 六项验收标准：

| 验收标准 | 方案当前达成度 | 缺口 |
|---------|--------------|------|
| 1. 6 条样例 case 全跑通，生成完整报告 | ⚠️ 部分可达成 | 受 §3.1/3.2/3.3/3.4 阻塞，修正后可达成 |
| 2. 隐藏样本决策准确率 ≥ 80% | ⚠️ 设计可行，缺自验手段 | 四维 gate 设计合理，但未提供离线自验集 |
| 3. 过拟合场景必须拒绝 | ✅ 可达成 | Gate 1（总分）+ Gate 2/3（分布）分层防御，修正 §4.5 表述即可 |
| 4. 失败归因准确率 ≥ 75% | ⚠️ 设计可行，证据提取依赖字符串匹配 | `diagnoseToolFailure`/`diagnoseRubricFailure` 靠解析 Reason 文本，鲁棒性弱；建议补充基于 criterion 类型的判定 |
| 5. fake mode 耗时 ≤ 3 分钟 | ✅ 可达成 | FakeEngine < 1ms/调用，总耗时 < 1s |
| 6. 报告含 baseline/candidate/delta/gate/理由 | ✅ 设计完备 | [阶段 6](赛题解答方案.md) 字段映射完整 |

**交付物覆盖度**：

| 交付物 | 方案是否覆盖 |
|-------|-----------|
| Go pipeline 入口 main.go | ✅ [阶段 0-7](赛题解答方案.md) |
| 样例 train/validation evalset、metrics、baseline prompt、PromptIter 配置、README | ✅ [阶段 0](赛题解答方案.md) + [2.3 目录建议](赛题解答方案.md) |
| 6 条评测 case（3 训练 + 3 验证，含三类情况） | ✅ [阶段 0](赛题解答方案.md) |
| optimization_report.json / .md 示例输出 | ✅ [阶段 6](赛题解答方案.md) |
| 300-500 字方案设计说明 | ✅ [第 5 章](赛题解答方案.md)（约 460 字，符合） |
| 单元测试覆盖 gate/delta/归因/报告 | ✅ [阶段 8](赛题解答方案.md) |

---

## 八、总体结论

### 8.1 方案可行性判定

**架构层面：可行。** 三层架构、八阶段划分、四维 gate、六类归因的设计与赛题要求高度对齐，复用现有 PromptIter engine + evaluation service 的思路正确。

**实现层面：当前版本不可直接落地。** 存在 6 处必修级技术问题（§3.1-3.6），涉及类型混淆、API 不可用、签名错误、字段错误、状态分裂，若不修正将导致代码无法编译或 pipeline 无法跑通。

### 8.2 是否能基于此方案完成赛题

**能，但需先完成修正闭环。** 建议按以下顺序处理：

1. **第一步（必修，阻塞实现）**：完成建议 1-5（统一类型、删除独立重跑、修正 engine.New/RunRequest、删除 Observer 收集、gate 改方式 B）；
2. **第二步（应修，影响完备性）**：完成建议 6-8（补充 Profile/Agent 构造、细化 FakeEngine 多角色、修正表述）；
3. **第三步（实现）**：按修正后的方案进入编码，优先跑通"最小 syncrun + FakeEngine"变体验证数据通路，再补全归因/delta/gate/报告组件；
4. **第四步（自验）**：构造 ≥ 10 条离线 case 验证 gate 决策准确率，确保隐藏样本达标。

### 8.3 优势与不足总结

**优势**：
- Gap 识别准确，复用思路合理，与赛题六阶段/六验收标准映射清晰；
- 四维 gate 与六类归因设计完备，测试计划详尽；
- 目录结构与产物设计符合赛题交付物要求。

**不足**：
- 对 engine 包与 evaluation 包的类型边界认识不足，存在三类型混淆；
- 对公共 API 能力边界认识不足，误判 profile 注入可行性；
- 示例代码多处与实际 API 签名/字段不符，需逐处修正；
- 对 FakeEngine 多角色、Profile 构造、成本采集等关键技术细节缺乏落地说明。

**结论：方案具备可落地的设计骨架，但需按本报告 §3、§6 完成必修与应修项后方可进入实现阶段。修正后可满足赛题全部功能需求与性能指标。**
